-- BigQuery ML × Gemini でEC顧客の購買予測モデルを構築する
-- 出典: articles/bigquery-ml-gemini-ec-purchase-prediction.md

CREATE OR REPLACE TABLE `${PROJECT}.${DATASET}.ec_user_features` AS
-- 上記のSELECT文をそのまま貼り付けてください
;
