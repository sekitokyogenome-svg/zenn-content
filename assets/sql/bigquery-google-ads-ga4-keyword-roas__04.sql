-- 出典: BigQueryでGoogle広告×GA4データを結合してキーワード別の真のROASを計算する
-- 記事: articles/bigquery-google-ads-ga4-keyword-roas.md（結果をLooker Studioで可視化するヒント）
-- ${PROJECT} / ${DATASET} は実行前に実値へ置換すること

CREATE OR REPLACE VIEW `your_project.reporting.keyword_roas_monthly` AS
(
  -- 上記の最終SELECT文をここに貼り付ける
);
